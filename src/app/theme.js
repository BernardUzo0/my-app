"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#D00000",   
    },
    secondary: {
      main: "#FFC300",   
    },
  }
});

export default theme;
